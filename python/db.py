# Databases
# import sqlalchemy
# import sqlite3

# MongoDB - open source, NoSQL
# good for large sets of distributed data
# can manage document-oriented information
# https://www.mongodb.com/developer/products/mongodb/top-4-reasons-to-use-mongodb/
# scale cheaper
# query faster - no joins required, query for a single document
# pivot easier - can easily change the shape, eg. add school to a User document
# program faster - docs map to data structures in programming langs
# example,
# user = db['Users'].find_one({"_id": userId})
# result = db['Users'].update_one({"_id": userId}, {"$set": user})



